namespace biblia
{
    class SearchValues
    {
        public string Title, Author, Year, Publisher;
        public SearchValues(string title, string author, string year, string publisher)
        {
            Title = title;
            Author = author;
            Year = year;
            Publisher = publisher;
        }
    }
}
